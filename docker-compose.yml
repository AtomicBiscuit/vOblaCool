version: '3'

services:
  bot_server:
    build:
      context: src/py-bot-server
      dockerfile: ./Dockerfile
    ports:
      - '${LOCAL_TELEGRAM_API_SERVER_PORT}:8081'
    env_file:
      - ./.env
